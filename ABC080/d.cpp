#include <bits/stdc++.h>
using namespace std;

int main(void){
    int N,C;
    cin >> N >> C;
    int time[C]={};
    for (size_t i = 0; i < N; i++)
    {
        int s,t,c;
        cin >> s >> t >> c;
    }
    return 0;
}